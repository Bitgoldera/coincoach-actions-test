# v0.4.8 — natural signal-detail layouts

- Keeps all entry, TP and stop values exact.
- Varies only line breaks and separators across captions.
- Supports `|`, `/`, `→`, bullets, two-line and compact one-line layouts.
- Removes no required trading level labels.
- Context line and `NFA. DYOR.` remain disabled by default.
- Screenshot and signal-selection logic are unchanged.
