# v0.4.2 — lower chart crop with controlled variation

- Shifts the final 4:5 crop slightly downward to show more candle-chart area.
- Adds a small random vertical offset per capture so screenshots do not always start at one identical coordinate.
- Clamps the shift so the timeframe row and right-side price scale remain inside the image.
- Keeps the v0.4.1 safety fix that avoids clicking Binance's clear-all-drawings control.
- Proof publishing remains disabled.
