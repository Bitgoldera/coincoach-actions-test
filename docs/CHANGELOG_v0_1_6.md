# Changelog v0.1.6

- Prevents mobile-web timeframe selection from mis-clicking Binance chart toolbar controls.
- Cancels and rejects the destructive “delete all drawings” confirmation before capture.
- Uses DOM-triggered timeframe selection instead of pointer-coordinate clicks.
- Adds page-level validation so a green workflow cannot accept this modal as a valid chart image.
- Includes the GitHub module-path fix (`python -m scripts.run_free_cloud`).
